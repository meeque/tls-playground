worker_processes 1;

daemon off;

events {
  worker_connections  1024;
}

http {

  keepalive_timeout 2s;

  server {
    # listen on the default HTTP port
    listen *:80 default_server;

    # the server name
    server_name play.meeque.de;

    # ...
    root acme-challenges/public;
  }
}
